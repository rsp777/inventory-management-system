<?xml version="1.0" encoding="UTF-8"?>
<persistence version="3.0"
             xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
                                 https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd">

    <persistence-unit name="InventoryPU" transaction-type="JTA">
        <!--
            JNDI name of the datasource configured in WildFly standalone.xml.
            Define a datasource with jndi-name="java:/InventoryDS" that points
            to jdbc:mysql://192.168.29.84/inventory (user: ravi).
        -->
        <jta-data-source>java:/InventoryDS</jta-data-source>

        <properties>
            <!-- Schema management: validate on deploy, never auto-create in production -->
            <property name="jakarta.persistence.schema-generation.database.action" value="none"/>

            <!-- Hibernate dialect -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQLDialect"/>

            <!-- SQL logging (mirrors former Spring Boot logging.level.org.hibernate.SQL=DEBUG) -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>

            <!-- Use Jakarta JTA platform provided by WildFly -->
            <property name="hibernate.transaction.jta.platform"
                      value="org.hibernate.engine.transaction.jta.platform.internal.JBossAppServerJtaPlatform"/>
        </properties>
    </persistence-unit>

</persistence>
